'use client';

import React from 'react';
import TableCell from '@mui/material/TableCell';

/**
 * Represents a table header cell for options.
 *
 * @returns A React element representing the table header cell for options.
 */
export const TableHeaderOption = () => {
  return <TableCell key="options" align="left" padding="none" />;
};
